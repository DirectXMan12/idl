group-version(group: "examples", version: "v1") {

// `#[abc]` before a declaration is a marker, specifying some special
// behavior.  Markers must be imported,
// except for those in the root k8s namespace, which are equivalent to
// `k8s::abc`.

// non-peristent kinds don't have objectmeta
@nonpersistent
kind WebhookRequest {

}

}
